export const selectOpenModal = (state) => state.favorites.openModal;

export const selectLikeCards = (state) => state.favorites.likeCards;

export const selectIsWatched = (state) => state.favorites.isWatched;
